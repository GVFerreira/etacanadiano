<style>
    header #nav {
        display: none !important;
    }
</style>
<main class="my-5" style="flex: 1 1 0">
    <section>
        <div class="container">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h1 class="p-0 m-0">Consulte os usuários</h1>
                    <a href="/admin/register-user" class="btn btn-success">ADICIONAR NOVO <b>+</b></a>
                </div>
                <div class="card-body d-flex justify-content-center flex-row flex-wrap">
                    {{#each users}}
                        <div class="my-2 px-2 py-4 border border-secondary rounded w-100">
                            <div class="w-100">
                                <p class="p-0 m-0">Nome:</p>
                                <p>{{name}}</p>
                                <hr>
                                <p class="p-0 m-0">E-mail:</p>
                                <p>{{email}}</p>
                                <hr>
                                <p class="p-0 m-0">Criado em:</p>
                                <p>{{#formatDate createdAt}}{{/formatDate}}</p>
                                <hr>
                                <div class="d-flex flex-row gap-3 w-50">
                                    <a href="/admin/edit-user/{{id}}" class="btn btn-warning">Editar</a>
                                    <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target=".exampleModal{{id}}">Excluir</button>
                                </div>
                            </div>
                            <div class="modal fade exampleModal{{id}}" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                                <div class="modal-dialog">
                                    <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title" id="exampleModalLabel">Você tem certeza?</h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                    </div>
                                    <div class="modal-body">
                                        <p>O cliente será excluído para sempre. Você tem certeza que deseja excluí-lo?</p>
                                        <p>{{name}}</p>
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Fechar</button>
                                        <a href="/admin/delete-user/{{id}}" class="btn btn-danger">Excluir</a>
                                    </div>
                                    </div>
                                </div>
                            </div> 
                        </div>
                    {{else}}
                        <h3>Não há registro de usuários</h3>
                    {{/each}}
                </div>
            </div>
        </div>
    </section>
</main>
<script>
    document.getElementById('link-logo-home').href = '/admin'

    var elementoPai = document.getElementById("header-container")
    var novoElementoFilho = document.createElement("div")
    novoElementoFilho.innerHTML = `
        <a href="/admin/register-user" class="mx-4" style="color: white">Registrar usuário</a>
        <a href="/admin/consult-users" class="mx-4" style="color: white">Consultar usuários</a>
        <a href="/admin/consult-payments" class="mx-4" style="color: white">Consultar pagamentos</a>
        <a href="/users/logout" class="ms-4 btn btn-danger text-upperccase fw-bold">Sair</a>
    `
    elementoPai.appendChild(novoElementoFilho)
</script>
