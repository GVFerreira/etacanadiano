<main style="flex: 1 1 0">
    <section class="section-1 full-width py-5">
        <div class="container">
            <h1>Acompanhar solicitação</h1>
            <hr>
            <div class="card">
                <div class="card-body">
                    <form action="/consultando-solicitacao" method="POST">
                        <h2>Verifique o status da sua solicitação</h2>
                        <p>Insira o seu e-mail, código ou N° passaporte para consultar o progresso da sua aplicação.</p>
                        <div class="row">
                            <div class="col">
                                <label for="EmailCod">Escolha:</label>
                                <select class="form-select w-50 mb-3" name="filtroSelecionado" id="filtroSelecionado" required>
                                    <option value="" selected disabled></option>
                                    <option value="email">E-mail</option>
                                    <option value="codigo">Código</option>
                                    <option value="passaporte">N° Passaporte</option>
                                </select>
                                <div class="input-group mb-3">
                                    <input type="text" class="form-control" id="codeInsert" name="codeInsert" placeholder="Digite seu e-mail, código ou número do passaporte" aria-label="Recipient's username" aria-describedby="button-addon2">
                                    <button class="btn btn-dark" type="submit" id="button-addon2">Buscar<i class="ms-2 bi bi-search"></i></button>
                                </div>
                            </div>
                            <div class="col">

                            </div>
                        </div>
                    </form>
                    <hr>
                    <section class="section-results">
                        {{#each search_result}}
                            <div class="px-3 py-4">
                                <p><b>Nome: </b>{{firstName}} {{surname}}</p>
                                <p><b>E-mail de contacto: </b>{{contactEmail}}</p>
                                <p><b>Telefone de contacto: </b>{{contactTel}}</p>
                                <p>
                                    <b>Pagamento: </b>
                                    {{#if pagamento.status}}
                                        {{#formatStatusPayment pagamento.status}}{{/formatStatusPayment}}
                                    {{else}}
                                        <span class="alert-danger p-1">Pagamento não efetuado</span>
                                    {{/if}}
                                </p>
                                <p><b>Situação Governo Canadiano: </b>{{statusETA}}</p>
                                {{#if messageClient}}
                                    <p class="mb-1"><b>Mensagem para você:</b></p>
                                    <textarea rows="5" class="form-control w-50 mb-2" disabled>{{messageClient}}</textarea>
                                {{/if}}
                                {{#if attachments}}
                                    {{#each attachments}}
                                        Clique aqui para fazer o download do seu documento: <a href="/consulta-download-documento/{{filename}}">{{filename}}</a>
                                    {{/each}}
                                {{/if}}
                            </div>
                        {{else}}
                            <h4 class="mb-5 text-center" style="color: #F00">Não há nenhum processo com esse código</h4>
                        {{/each}}
                    </section>
                </div>
            </div>
            
        </div>
    </section>
</main>
<link rel="stylesheet" href="./css/acompanhar.css">